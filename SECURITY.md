# Security Policy

This crate does not execute arbitrary commands by itself. It computes impact and verification plans from manifests.

If you find a security issue in parsing, CLI behavior, or example assets, report it privately before opening a public issue.
